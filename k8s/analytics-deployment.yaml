apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service-deployment
  labels:
    app: analytics-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: analytics-service
  template:
    metadata:
      labels:
        app: analytics-service
    spec:
      containers:
      - name: analytics-service
        image: gcr.io/YOUR_GCP_PROJECT_ID/analytics-service:latest # REPLACE with your actual image path
        ports:
        - containerPort: 8000
        env:
        - name: REDIS_HOST
          value: "redis"
